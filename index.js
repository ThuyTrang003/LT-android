/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';
import thuyTrang from './thuyTrang';
AppRegistry.registerComponent(appName, () => thuyTrang);
